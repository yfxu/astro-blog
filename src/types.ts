export type Site = {
  TITLE: string;
  ALT_TITLE: string;
  DESCRIPTION: string;
  EMAIL: string;
};

export type Metadata = {
  TITLE: string;
  DESCRIPTION: string;
};

export type SocialLink = {
  name: string;
  url: string;
};
